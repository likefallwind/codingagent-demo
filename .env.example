# 复制成 .env 后填入。.env 已被 .gitignore 忽略，不会提交。
MINIMAX_API_KEY=

# 可选：覆盖默认端口
# PORT=8787        # 接口服务
# WEB_PORT=5173    # 开发模式的前端

# 可选：覆盖模型。交互路径用 highspeed 变体（延迟低），批量/生成用强模型。
# MINIMAX_MODEL_FAST=MiniMax-M2.7-highspeed
# MINIMAX_MODEL_STRONG=MiniMax-M3
